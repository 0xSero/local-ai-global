# CLI

The local workflow: detect hardware, list compatible recipes, choose in a TUI, and resolve the selected graph. It reads `packages/registry` directly by default and can be pointed at another checkout with `LOCAL_AI_REGISTRY_DIR`.
